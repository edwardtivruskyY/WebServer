说明文档
与聊天室的服务端结构一样，使用多线程来完成对多个客户端链接的支持。让主线程只负责接收客户端的
TCP链接，而实际的交互工作交给线程ClientHandler来完成。

实现：
1.在com.webserver.core下新建一个类：ClientHandler，用于基于HTTP协议与指定客户端交互
2.在Webserver类中，当接受一个客户端链接后，启动该线程与其交互。

由于请求中的请求行和消息头都是由一行字符串构成的(以回车加换行符结尾),因此实现一个算法完成读
取一行字符串的操作，便于后期解析请求时使用。
